import { Typehinted } from '../server-api/server-protocol';
/**
 * A running and connected ensime client
 *
 * low-level api
 */
export interface ServerConnection {
    destroy: () => void;
    httpPort: string;
    /**
     * Post a msg object
     */
    post: (msg: any) => PromiseLike<Typehinted>;
}
export declare function createConnection(httpPort: string, generalMsgHandler: any, serverVersion: any, serverPid?: any): PromiseLike<ServerConnection>;
