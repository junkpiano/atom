import { DotEnsime } from '../types';
import { ServerConnection } from '../server-api/server-connection';
import { Api } from '../server-api/server-api';
export interface EnsimeInstance<UI> {
    destroy(): any;
    rootDir: string;
    httpPort: string;
    api: Api;
    dotEnsime: DotEnsime;
    isSourceOf(path: string): boolean;
    /** Client specific ui to use for ui switching and stuff */
    ui: UI;
}
export declare function makeInstanceOf<T extends {
    destroy(): void;
}>(dotEnsime: DotEnsime, connection: ServerConnection, ui: T): EnsimeInstance<T>;
