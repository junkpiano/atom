import * as WebSocket from "ws";
export interface NetworkClient {
    destroy(): any;
    send(msg: string): any;
}
export declare class TcpClient implements NetworkClient {
    destroy(): void;
    send(msg: string): any;
}
export declare class WebsocketClient implements NetworkClient {
    websocket: WebSocket;
    constructor(httpPort: string, onConnected: () => any, onMsg: (msg: string) => any, serverVersion?: string);
    destroy(): void;
    send(msg: string): void;
}
